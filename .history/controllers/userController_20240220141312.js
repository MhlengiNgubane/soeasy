const User = require("../models/User");

module.exports = {
    getUser: async(req, res) => {
        try {
            
        } catch (error) {
            const User = require("../models/User");
        }
    }
}