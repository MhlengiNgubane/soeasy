const Food = require('../models/Food');

module.exports = {
    addFood: async (req, res) => {
        const { ti} = req.body;
        try {
            
        } catch (error) {
            
        }
    },
};